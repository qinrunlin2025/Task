# Git学习笔记

## 1.使用 Git Bash 提交并推送名为 `hello.md` 的文档到远程仓库，内容为 `# Hello + 你的姓名`。

1. 严格按照这里的步骤来！

[]([保姆级教程，教你使用Git推送你的项目到Github_git push github-CSDN博客](https://blog.csdn.net/jiunian_2761/article/details/122907766))

2. **注意事项**

   2.1新建一个仓库，传到新仓库

   2.2 ssh-key找不到没关系，点开setting找到key就行，密钥上传公钥pub

   2.3字别打错了，特别是origin

   2.4别用ai，他会引入歧途

   2.5千万不要灵机一动跳步骤，不然每一块都能错的离谱

3. 以上都是血一样的教训，因为又整了半天。

## 2.我们在当前仓库的 `first_half_tasks` 分支下留有一个名为 `git-idea` 的文件夹, 你们可以将你们的 Git 学习感想(记得标注名字)放进来,并向我们提 PR。

+ 其实就是点开左上角git idea那个文件夹，再add file，把感想传上去
+ 紧接着点击pull request就行